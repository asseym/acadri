<%= form_for @program_date, :html => { :class => "form-horizontal program_date" } do |f| %>

    <% if @program_date.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@program_date.errors.count, "error") %> prohibited this program_date from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @program_date.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label :date, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :date, :class => 'form-control' %>
    </div>
    <%= error_span(@program_date[:date]) %>
  </div>


  <%= f.submit nil, :class => 'btn btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
            program_dates_path, :class => 'btn btn-default' %>

<% end %>
