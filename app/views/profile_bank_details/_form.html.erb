<%= form_for(@profile_bank_detail) do |f| %>
  <% if @profile_bank_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@profile_bank_detail.errors.count, "error") %> prohibited this profile_bank_detail from being saved:</h2>

      <ul>
      <% @profile_bank_detail.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :bank_details %><br>
    <%= f.text_area :bank_details %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
